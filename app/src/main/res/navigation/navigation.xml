<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/todayWeatherFragment">

    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.weather.MainActivity"
        android:label="main_activity"
        tools:layout="@layout/main_activity" />
    <fragment
        android:id="@+id/todayWeatherFragment"
        android:name="com.example.weather.CurrentWeather.TodayWeatherFragment"
        android:label="today_weather_fragment"
        tools:layout="@layout/today_weather_fragment" />
    <fragment
        android:id="@+id/weatherForecastFragment"
        android:name="com.example.weather.WeatherForecast.WeatherForecastFragment"
        android:label="weather_forecast_fragment"
        tools:layout="@layout/weather_forecast_fragment" />
</navigation>